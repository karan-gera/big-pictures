name = "album-search-api"
main = "src/index.js"
compatibility_date = "2024-01-01"
keep_vars = true

# Environment variables/secrets are set in the Cloudflare Dashboard
# keep_vars = true ensures they are preserved during deployment 